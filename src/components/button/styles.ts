import colors from "src/assets/colors";

const styles = {
  button: {
    backgroundColor: colors.white,
    borderStyle: "solid",
    borderWidth: 1,
    borderRadius: 10,
    borderColor: "#bdbdbd",
    height: 40,
    width: 40,
  },
};

export default styles;
